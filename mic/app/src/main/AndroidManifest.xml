<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        MODIFY_AUDIO_SETTINGS — normal permission (no runtime dialog).
        Required for AudioManager.isSpeakerphoneOn, AudioManager.mode changes.
    -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!--
        BLUETOOTH — normal permission on API <= 30 (classic Bluetooth detection).
        maxSdkVersion="30" ensures it is NOT requested on API 31+ where it is replaced
        by BLUETOOTH_CONNECT.
    -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />

    <!--
        BLUETOOTH_CONNECT — runtime permission on API 31+ (Android 12+).
        Required to read Bluetooth device names via AudioDeviceInfo on some devices.
        If denied, the app gracefully falls back to generic type labels.
        Note: AudioManager.getDevices() itself does NOT require this permission;
        it is only needed for richer product name resolution.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mic">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Mic"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>